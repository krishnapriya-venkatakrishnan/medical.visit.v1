/**
 * Synthetic fixtures - the only data this prototype ever uses (non-negotiable #3:
 * synthetic data only, no real personal or health data, ever).
 *
 * Conventions for fixtures added in later stages:
 *  - Names are obviously fake.
 *  - Values are plausible but invented.
 *  - Each fixture that feeds the AI carries stable IDs so generated findings can
 *    reference exact data points for provenance.
 */

export {};
